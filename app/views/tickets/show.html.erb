<div id="ticket">
	<h2><%= @ticket.title %></h2>
	<%= simple_format(@ticket.description) %>
</div>

<%= link_to "Edit Ticket", [:edit, @project, @ticket] %>
<%= link_to "Delete", [@project, @ticket], method: :delete, data: { confirm: "Are you sure about this?" } %>